# Session Plan -- 2026-03-13

## Completed This Session
- Implemented the Two-Camera Match Mode plan
- Created mode selection landing page (`/`) with Treino and Jogo options
- Moved existing single-camera dashboard from `/` to `/training`
- Created match dashboard at `/match` with:
  - Config panel for entering both board IPs (auto-detects from `/status`)
  - Side-by-side MJPEG camera feeds
  - Unified scoreboard (HOME x AWAY) with correct goal attribution
  - Unified controls: Start/Pause/Resume/Reset/End sent to both boards
  - Per-board calibration buttons
  - Merged goal log with VAR review per board
  - Offline resilience via `Promise.allSettled`
- Extended `/status` JSON with `role` and `peer` fields
- Added `BOARD_ROLE` and `PEER_IP` env vars to `load_env.py`
- Bumped `max_uri_handlers` from 14 to 16

## Files Changed
- `src/mode_select.h` — New: mode selection page HTML
- `src/match_dashboard.h` — New: match dashboard HTML/CSS/JS (~340 lines)
- `src/web_stream.cpp` — New routes, status fields, handler refactor
- `load_env.py` — Added BOARD_ROLE, PEER_IP to allowed env vars
- `.env` — Added commented-out BOARD_ROLE/PEER_IP templates

## Next Steps / Recommendations
- Install PlatformIO and verify build compiles (`pio run`)
- Test on actual hardware with two boards
- Consider adding sound effects on goal (MAX98357 speaker is available)
- Add match timer / half tracking to match dashboard
- Consider mDNS discovery so boards auto-find each other instead of manual IP entry
- Add persistent match history (localStorage or SD card)
