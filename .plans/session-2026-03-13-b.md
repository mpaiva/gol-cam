# Session Plan -- 2026-03-13 (session 2)

## Summary of Previous Session
- Two-camera match mode was implemented with mode selector, training dashboard, and match dashboard
- All routes and endpoints working, build passing

## Completed This Session
- Implemented Multi-Language Support (i18n) plan for EN and PT
- Extracted training page from inline C string concatenation in `web_stream.cpp` to `training_dashboard.h` as raw string literal (Step 0)
- Added i18n system to all 3 pages with consistent pattern:
  - Language picker (EN/PT buttons) fixed top-right
  - `I18N` dictionary object with `en` and `pt` keys
  - `t(key, ...args)` function with `%d`/`%s` substitution
  - `setLang(lang)` applies `data-i18n` attributes + updates picker highlight
  - Auto-detect from `navigator.language`, persist in `localStorage('gol-lang')`
- Mode select page: 8 strings translated (Training/Treino, Match/Jogo, descriptions)
- Training dashboard: ~25 strings (buttons, badges, hints, goal log, VAR)
- Match dashboard: ~30 strings (config panel, scoreboard labels, controls, goal log, VAR)
- Build verified: SUCCESS (822KB flash, 17.2% RAM)

## Files Changed
- `src/training_dashboard.h` — New: training page extracted as raw string literal with i18n
- `src/web_stream.cpp` — Removed inline training HTML (~260 lines), replaced with `#include` and 4-line handler
- `src/mode_select.h` — Added i18n dictionary, `data-i18n` attrs, language picker
- `src/match_dashboard.h` — Added i18n dictionary, `data-i18n` attrs, `t()` calls, language picker
- `.about/feature-requests.md` — Added F5: Multi-Language Support

## Next Steps / Recommendations
- Test on hardware to verify all pages render correctly
- Add a `<a>` back-link on training page (currently has none — added `data-i18n='nav.menu'`)
- Consider adding Spanish (es) as third language
- Consider extracting shared i18n code (t function, setLang, picker CSS) into a common snippet to reduce duplication
- Sound effects on goal detection (MAX98357 speaker available)
- Match timer / half tracking
- mDNS discovery for auto board pairing
