# Session Plan -- 2026-05-24

## Completed This Session
Consolidated three sibling projects into the `gol-cam` repository under the `consolidation` branch, following `consolidation-plan.md`.

### Phase 0 — Baseline
- Created `consolidation` branch from `master` (both at `ab35f14`)
- Added `.claude/` to `.gitignore`
- Verified `pio run -e dfr1154` baseline build still passes (Flash 55.4%, RAM 17.2%)

### Phase 1 — 3D models from `GOOOL Elatronico`
- Imported 24 `.stl` / `.3mf` files (~56 MB) into `hardware/`
- Organized into `trave/`, `caixa-camera/`, `placar/`, `acessorios/`, `arquivos-historicos/`
- Wrote `hardware/README.md` with per-category descriptions and slicer hints
- Source was not a git repo — plain copy

### Phase 2 — Subtree merge of `Placar Eletronico`
- Added `Placar Eletronico` as a temporary git remote, fetched, and used `merge -s ours --allow-unrelated-histories` + `read-tree --prefix=_import_placar/` to preserve the 2 original commits (`d4b5221`, `ceee6dc`)
- Brought in `New Scene.3mf` (modified, unstaged) and `Placar ESP 32 Wifi.3mf` (untracked) as a follow-up commit
- Removed the temporary remote

### Phase 3 — Redistribute imported files
- Screenshots → `.reports/screenshots/placar-*.{png,jpeg}`
- 3D models → `hardware/placar/`
- Sketch → `legacy/Placar_Eletronico_Wi-Fi.ino` (later deleted in Phase 5)
- Original README → `.plans/placar-original-readme.md` (deleted in Phase 7 after content folded into top-level README)
- Deviation from plan: moved the `.ino` to `legacy/` (outside `src/`) instead of `src/_legacy_placar.ino` to avoid breaking the PlatformIO build before the placar lib was set up

### Phase 4 — Dual-environment build
- Reshaped `platformio.ini` with two envs:
  - `[env:dfr1154]` — camera, default env
  - `[env:placar]` — ESP32 DevKit V1, `majicdesigns/MD_MAX72XX@^3.5.0`
- `build_src_filter` on each env to isolate sources
- Extended `load_env.py` to also inject `CAMERA_IP` and `SCOREBOARD_IP`
- Stub `src/main_placar.cpp` so the placar env links

### Phase 5 — Port the Arduino sketch
- Created `include/pins_placar.h`, `placar_display.h/.cpp`, `placar_buttons.h/.cpp`, `placar_web.h/.cpp`
- Rewrote `main_placar.cpp` with WiFi STA→AP fallback and camera polling
- Deleted `legacy/` (sketch now lives only in git history)

### Phase 6 — Cross-board sync
- Camera now exports `"side":"A|B"` in `/status` (derived from `BOARD_ROLE`)
- New `goalPushTask` in `main.cpp`: posts `POST http://${SCOREBOARD_IP}/goal {"side":...}` after detection (off the detection loop)
- Placar gained `POST /goal {"side":"A|B"}` endpoint that increments
- Polling in the placar fixed to read `"goals"` (was `"goalCount"`)
- Match dashboard: 3rd IP field "Placar:", persisted in `localStorage`, `pollPlacar()` shows `"Placar: online (A x B)" / "Placar: offline"`

### Phase 7 — Documentation
- Rewrote `README.md`: system overview with network diagram, BOM for both boards, placar wiring tables, dual-env build commands, repo layout
- Updated `CLAUDE.md`: two-board hardware, both envs in build commands, cross-board sync protocol section, key modules split by board
- Reordered `.about/feature-requests.md` (F1–F5 + new F6 placar, F7 standalone fallback)
- Deleted `.plans/placar-original-readme.md` (content folded into top-level README)

## Files Changed (full list)
- `platformio.ini`, `load_env.py`, `.gitignore`
- `README.md`, `CLAUDE.md`
- `.about/feature-requests.md`
- `.plans/consolidation-plan.md` (new), `.plans/session-2026-05-24.md` (new)
- `hardware/` (entire new directory, 26 files)
- `.reports/screenshots/placar-*` (4 new images)
- `include/pins_placar.h`, `placar_display.h`, `placar_buttons.h`, `placar_web.h` (new)
- `src/main.cpp` — goal push task + scoreboard side derivation
- `src/web_stream.cpp` — `/status` now includes `"side"`
- `src/match_dashboard.h` — placar IP field + polling
- `src/main_placar.cpp` — full implementation (was stub)
- `src/placar_display.cpp`, `placar_buttons.cpp`, `placar_web.cpp` (new)

## Build status
- `pio run -e dfr1154` → SUCCESS (Flash 55.4%, RAM 17.2%)
- `pio run -e placar` → SUCCESS (Flash 60.3%, RAM 13.8%)

## Next Steps (Phase 8 — hardware validation)
- Flash `dfr1154` to a DFR1154 and verify the training and match dashboards still work end-to-end
- Build the placar hardware (ESP32 DevKit + 4× MAX7219 chained + 4 buttons) and flash `placar`
- Verify:
  - 0×0 on boot, both displays render correctly oriented
  - Physical buttons increment / reset their side
  - Same WiFi → STA mode; wrong WiFi → AP fallback within 15 s
  - Camera goal triggers placar increment within 1 s via push
  - Polling fallback works if push fails (block camera→placar firewall and confirm)
  - Match dashboard shows "Placar: online (N x M)" when configured
- Tag `v1.0-consolidated`

## Phase 9 (post-validation)
After Phase 8 passes in hardware, ask the user whether to archive (or delete) the original `C:\_PROJETOS\Placar Eletronico` and `C:\_PROJETOS\GOOOL Elatronico` directories. Do not remove them without explicit approval — the placar's `.git` would be lost permanently.
